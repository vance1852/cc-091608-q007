# 腕戴数据监管链

该项目保存临床研究中的设备发放流转、站点数据包及冻结批次资料。受试者以研究代号表示，随机分组信息与普通站点操作隔离。

主要契约位于 `src/contracts.ts`。`fixtures/site-uploads.json` 记录一段跨站换机、内容重复和校正包上传的脱敏过程，摘要值仅用于说明数据关系。

工程使用 Node.js 22 与 TypeScript，安装依赖后运行 `npm test` 可完成契约检查。
